import {Client, Interaction, SlashCommandBuilder, GatewayIntentBits, REST, Routes} from "discord.js";

export default (client: Client): void => {
    client.on("interactionCreate", async (interaction: Interaction) => {

    })
}
